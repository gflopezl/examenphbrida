import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.examenphbrida',
  appName: 'examenphbrida',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
